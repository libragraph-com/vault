/**
 * Pure Java value types shared across all Vault modules.
 *
 * <p>Future home of BlobRef and other core domain types.
 * ContentHash and buffer types live in {@code shared/utils}.
 * This module has no framework dependencies — only SLF4J for logging.
 */
package com.libragraph.vault.types;
