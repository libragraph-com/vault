/**
 * Shared utilities for all Vault modules.
 *
 * <p>Contains {@link com.libragraph.vault.util.ContentHash} (BLAKE3-128) and the
 * {@link com.libragraph.vault.util.buffer buffer layer} (BinaryData, Buffer, RamBuffer).
 * No framework dependencies — pure Java.
 */
package com.libragraph.vault.util;
